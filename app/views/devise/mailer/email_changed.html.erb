<p>Hello <%= @email %>!</p>

<p>
  We're contacting you to notify you that your email for
  <%= Rails.application.class.parent_name.downcase %> has been
  changed to
  <% if @resource.try(:unconfirmed_email?) %>
    <%= @resource.unconfirmed_email %>.
  <% else %>
    <%= @resource.email %>.
  <% end %>
</p>
