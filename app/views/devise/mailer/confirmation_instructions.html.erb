<p>Welcome <%= @email %>!</p>

<p>
  You can confirm your <%= Rails.application.class.parent_name.downcase %>
  account email through the link below:
</p>

<p><%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token) %></p>
